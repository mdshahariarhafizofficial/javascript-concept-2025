// Create two arrays of your choice.
const marks = [45, 35, 78];
const names = ["Rohim", "Korim", "Jasim"]

// Use the concat method to combine the two arrays into a new array.
let result = names.concat(marks);

// Print both the original arrays and the combined array using console.log().
console.log(marks);
console.log(names);
console.log(result);

let h = "Hello";
let w = "World"
let output = h.concat(" ", w)
console.log(output);

